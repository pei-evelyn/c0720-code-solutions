/* eslint-disable no-unused-vars */
const chunk = (array, size) => {
  if (array.length === 0) {
    return array;
  }
  const leftOverArr = [];
};

// return one central array with indexes of smaller arrays
